{
  "name": "@onting/selenium-webdriver-message-port-integration-test",
  "version": "0.0.0-0",
  "private": true,
  "description": "",
  "license": "MIT",
  "author": "",
  "type": "module",
  "scripts": {
    "build": "npm run build:copy && npm run build:custom --if-present",
    "build:copy": "mkdir -p ./webDriver/static/; SRC=$(node --eval=\"console.log(require('path').resolve(require('resolve-cwd')('@onting/selenium-webdriver-message-port'), '../../dist'))\"); DEST=$(realpath ./webDriver/static); rm -rf $DEST; mkdir -p $DEST/js/; cp $SRC/** $DEST/js/",
    "bump": "npm run bump:prod && npm run bump:dev",
    "bump:dev": "PACKAGES_TO_BUMP=$(cat package.json | jq -r '(.pinDependencies // {}) as $P | (.localPeerDependencies // {}) as $L | (.devDependencies // {}) | to_entries | map(select(.key as $K | $L | has($K) | not)) | map(.key + \"@\" + ($P[.key] // [\"latest\"])[0]) | join(\" \")') && [ ! -z \"$PACKAGES_TO_BUMP\" ] && npm install $PACKAGES_TO_BUMP || true",
    "bump:prod": "PACKAGES_TO_BUMP=$(cat package.json | jq -r '(.pinDependencies // {}) as $P | (.localPeerDependencies // {}) as $L | (.dependencies // {}) | to_entries | map(select(.key as $K | $L | has($K) | not)) | map(.key + \"@\" + ($P[.key] // [\"latest\"])[0]) | join(\" \")') && [ ! -z \"$PACKAGES_TO_BUMP\" ] && npm install $PACKAGES_TO_BUMP || true",
    "docker": "docker compose --file ./docker-compose.yml up --quiet-pull",
    "docker:hub": "docker compose --file ./docker-compose.hub.yml up --quiet-pull --scale chromium=4",
    "init": "npm run init:dev --if-present && npm run init:prod --if-present",
    "init:dev": "npm install --save-dev @happy-dom/global-registrator @tsconfig/strictest expect resolve-cwd typescript",
    "init:prod": "npm install --save @onting/test-harness@0.0.0-0 @onting/selenium-webdriver-message-port@0.0.0-0",
    "posttest": "if test \"$CI\" = \"true\"; then docker compose logs; docker compose down; fi",
    "precommit": "ESLINT_USE_FLAT_CONFIG=false eslint --ext .js,.jsx,.cjs,.mjs,.ts,.tsx,.cts,.mts ./",
    "pretest": "if test \"$CI\" = \"true\"; then npm run build; npm run docker -- --detach --wait; fi",
    "switch": "cat package.json | jq --arg SWITCH_NAME $SWITCH_NAME -r '(.[\"switch:\" + $SWITCH_NAME] // {}) as $TEMPLATE | .devDependencies += ($TEMPLATE.devDependencies // {}) | .dependencies += ($TEMPLATE.dependencies // {})' | tee ./package.json.tmp && mv ./package.json.tmp ./package.json",
    "test": "npm run test:unit && npm run test:types",
    "test:types": "node --experimental-strip-types --import @onting/test-harness/test-types.import --test **/*.test-types.ts",
    "test:unit": "node --experimental-strip-types --experimental-test-coverage --experimental-test-isolation=none --test"
  },
  "pinDependencies": {},
  "localPeerDependencies": {
    "@onting/selenium-webdriver-message-port": "0.0.0-0",
    "@onting/common": "0.0.0-0",
    "@onting/test-harness": "0.0.0-0"
  },
  "devDependencies": {
    "@happy-dom/global-registrator": "^20.9.0",
    "@onting/selenium-webdriver-message-port": "^0.0.0-0",
    "@testduet/given-when-then": "^0.1.0",
    "@testduet/wait-for": "^0.1.1",
    "@tsconfig/strictest": "^2.0.8",
    "@types/node": "^25.9.1",
    "expect": "^30.4.1",
    "message-port-rpc": "^2.0.1",
    "resolve-cwd": "^3.0.0",
    "selenium-webdriver": "^4.44.0",
    "typescript": "^6.0.3"
  },
  "files": [
    "**/*.cjs",
    "**/*.js",
    "**/*.mjs",
    "webDriver/**/*"
  ],
  "exports": {},
  "dependencies": {
    "@onting/common": "^0.0.0-0",
    "@onting/test-harness": "^0.0.0-0"
  }
}
